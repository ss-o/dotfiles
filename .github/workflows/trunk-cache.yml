---
name: "⭕ Trunk [cache]"
on:
  push:
    paths: [.trunk/trunk.yaml]
  workflow_dispatch: {}

concurrency:
  group: ${{ github.head_ref || github.run_id }}
  cancel-in-progress: true

permissions: read-all

jobs:
  cache:
    runs-on: ubuntu-latest
    permissions:
      actions: write
    steps:
      - name: ✅ Checkout
        uses: actions/checkout@v4
      - name: ✨ Trunk Check
        uses: trunk-io/trunk-action@057aaa4e827d524f2b6041c50dc260bd1e11e271
        with:
          check-mode: populate_cache_only
